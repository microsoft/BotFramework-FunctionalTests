parameters:
  scenarios: []

stages:
  - ${{ each scenario in parameters.scenarios }}:
    - stage: 
      displayName: Test ${{ scenario }} Scenario
      jobs:
        - job: Test
          steps:
            - template: configureConsumers.yml
              parameters:
                scenario: ${{ scenario }}

            - task: UseDotNet@2
              displayName: 'Use .Net Core sdk 3.1.x'
              inputs:
                version: 3.1.x

            - task: DotNetCoreCLI@2
              displayName: 'Dotnet Test'
              inputs:
                command: test
                projects: 'Tests/SkillFunctionalTests/SkillFunctionalTests.csproj'
                arguments: '-v n --configuration $(BuildConfiguration) --filter TestCategory!=IgnoreInAutomatedBuild&TestCategory=${{ scenario }}'
