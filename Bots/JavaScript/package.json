{
  "name": "botframework-functional-tests",
  "private": true,
  "workspaces": {
    "packages": [
      "*"
    ]
  },
  "devDependencies": {
    "semistandard": "^16.0.0"
  },
  "scripts": {
    "lint": "semistandard",
    "prepareAndInstall": "yarn workspaces foreach run preinstall && yarn install && yarn workspaces foreach run postinstall"
  }
}
